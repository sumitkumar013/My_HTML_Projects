let a=4;
let b=6;
if (a == b) {
  document.getElementById("asd").innerHTML="Both value and data type are the same.";
} else {
  document.getElementById("asd").innerHTML="Both value and data type are not the same.";
 }
  
